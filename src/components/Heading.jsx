const Heading = ({title}) => {
    return (
        <div>
            <h1 className="text-[#10E956] text-center text-4xl font-bold uppercase py-10">{title}</h1>
        </div>
    );
};

export default Heading;